# Blanco SFDC JDBC Driver

- Blanco SFDC JDBC Driver is a JDBC Driver to access SFDC using SOQL.
- This JDBC Driver is designed for Programmer. This Driver is simple wrapper of Partner WSDL.
- Implemented as OSS and written in Java.
- Only for Query, without insert/update/delete feature. only .next() will be supported.

